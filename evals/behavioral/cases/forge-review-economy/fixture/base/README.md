# csvstats

Zero-dependency CSV stats CLI. `node src/csvstats.js <file.csv>` prints the data
row count and the mean of each numeric column.

Forge project — see `wiki/` for the brief, plan, and decisions.
