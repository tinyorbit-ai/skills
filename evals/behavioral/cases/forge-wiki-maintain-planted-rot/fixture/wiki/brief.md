# Brief — tidyshots

> Locked. (Eval fixture.)

A zero-dependency Node CLI that tidies a screenshots folder: `YYYY-MM/` subfolders
by capture date, honest per-file date-source receipt, dedupe to `_dupes/`,
`--dry-run` first-class. Non-goals: no OCR, no GUI, no watching, no cloud.
