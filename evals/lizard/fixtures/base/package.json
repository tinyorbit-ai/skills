{
  "name": "orbitcart",
  "version": "0.3.0",
  "private": true,
  "description": "A small order and catalog service.",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "lint": "eslint src"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "vitest": "^1.5.0"
  }
}
