feat(catalog): show archived releases in the product switcher

The dashboard switcher now includes archived releases and marks the current release.
Opening it still puts the current release in view, so operators can tell which
release they are editing even when a product has years of history.
