fix(catalog): auto-select the current product in the dashboard switcher

The product switcher now selects its highlighted card when it mounts. Storefront and
repeat-product preview cards keep their existing behavior; the effect is limited to
the dashboard switcher.
