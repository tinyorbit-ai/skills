.DS_Store
node_modules/
*.log
.env
.env.*
!.env.example

# local scratch — HTML reports, plan-bench clones, throwaway fixtures
tmp/

# Codex local state (AGENTS.md is the tracked entry point)
.codex/
